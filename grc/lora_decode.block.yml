# auto-generated by grc.converter

id: lora_decode
label: LoRa Decoder
category: '[lora]'

parameters:
-   id: spreading_factor
    label: Spreading Factor
    dtype: int
    default: '8'
-   id: header
    label: Header
    dtype: bool
    default: 'True'
-   id: payload_len
    label: Payload Length
    dtype: int
    default: '4'
-   id: code_rate
    label: Code Rate
    dtype: int
    default: '1'
-   id: crc
    label: CRC
    dtype: bool
    default: 'True'
-   id: low_data_rate
    label: Low Data Rate
    dtype: bool
    default: 'False'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
-   domain: message
    id: header

templates:
    imports: import lora
    make: lora.decode(${spreading_factor}, ${header}, ${payload_len}, ${code_rate},
        ${crc}, ${low_data_rate})

file_format: 1
