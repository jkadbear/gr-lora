# auto-generated by grc.converter

id: lora_encode
label: LoRa Encoder
category: '[lora]'

parameters:
-   id: spreading_factor
    label: Spreading Factor
    dtype: int
    default: '8'
-   id: code_rate
    label: 'Code Rate / # Parity Bits'
    dtype: int
    default: '4'
-   id: crc
    label: CRC
    dtype: bool
    default: 'False'
-   id: low_data_rate
    label: Low Data Rate
    dtype: bool
    default: 'False'
-   id: header
    label: Explicit Header
    dtype: bool
    default: 'False'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import lora
    make: lora.encode(${spreading_factor}, ${code_rate}, ${crc}, ${low_data_rate},
        ${header})

file_format: 1
