# auto-generated by grc.converter

id: lora_pyramid_demod
label: Pyramid Demodulator
category: '[lora]'

parameters:
-   id: spreading_factor
    label: Spreading Factor
    dtype: int
    default: '8'
-   id: low_data_rate
    label: Low Data Rate
    dtype: bool
    default: 'False'
-   id: beta
    label: FFT Window Beta
    dtype: float
    default: '25.0'
-   id: fft_factor
    label: FFT Size Factor
    dtype: int
    default: '2'
-   id: threshold
    label: Peak Height Threshold
    dtype: float
    default: '0.005'
-   id: fs_bw_ratio
    label: Samp-BW ratio
    dtype: float
    default: '8'

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: header

outputs:
-   domain: message
    id: out

templates:
    imports: import lora
    make: lora.pyramid_demod(${spreading_factor}, ${low_data_rate}, ${beta}, ${fft_factor},
        ${threshold}, ${fs_bw_ratio})

file_format: 1
